# PAUL Handoff

**Date:** 2026-05-30
**Status:** paused — human-verify checkpoint pending for 04-03

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** LAYERZ — a layer-oriented groovebox (JUCE/C++ desktop app + VST3/AU/LV2 plugin)
**Core value:** Producers get the immediacy of a groovebox and the aesthetic reach of a full DAW plugin chain — without the friction of assembling one.

---

## Current State

**Version:** 0.3.0 (F2 complete, F3 in APPLY)
**Phase:** 4 of 8 — F3 Groove Engine
**Plan:** 04-01 ✓, 04-02 ✓, 04-03 APPLY COMPLETE — awaiting human-verify checkpoint

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [APPLY done, checkpoint pending, then UNIFY]
```

---

## What Was Done This Session

- Phase 3 (F2 BASS + Multi-pattern) fully UNIFIED, transitioned to Phase 4
- Sample banks registered in ROADMAP.md (local dir + .layerzbank JSON format, v2 scope)
- **04-01 (F3.99 spike)**: Strategy B carry-forward queue chosen; user confirmed
- **04-02**: AestheticResolver (DRAG/PUSH/ROLL/STUTTER/FRACTURE), MonoSynth ADSR, kQueueCap=64, triggerRetain() for STUTTER click-free, Clock::samplesPerBeat() accessor, BeatSequencer uses stepPeriodSamples
- **04-03**: Template library (5 BEAT + 3 BASS presets), applyMorph(), findTemplate() with null guard, GroovePanel UI in BeatLayerStrip + BassLayerStrip
- LAYERZ standalone launched and running for verification

---

## What's In Progress

- **Human-verify checkpoint (04-03)**: LAYERZ is open. User needs to test groove engine and provide "approved" signal.
  - Test: Lurch Soul + morph 1.0 → snare late (DRAG)
  - Test: Fracture Grid + morph 1.0 → ~50% hats
  - Test: Roll Fury + morph 1.0 → hats ×4
  - Test: Stutter Bass (BASS strip) + morph 1.0 → bass stutters
  - Test: morph=0 → neutral (no effect)
  - F3-AC7: 30-second validation recording (subjective — user confirms feel)

---

## What's Next

**Immediate:** Resume user's groove engine verification test.
- Launch LAYERZ: `open build/LAYERZ_artefacts/Standalone/LAYERZ.app`
- Ask user: "Did you test the groove engine? Type 'approved' + describe the feel, or describe issues."

**After approval:**
- Write 04-01-SUMMARY.md, 04-02-SUMMARY.md, 04-03-SUMMARY.md
- Update STATE.md to APPLY complete
- Run /paul:unify to close Phase 4 loop
- Transition to Phase 5 (F4): HARMONIC + MELODIC layers

---

## Key Decisions Made

| Decision | Impact |
|----------|--------|
| AestheticResolver Strategy B (carry-forward queue) | DRAG defers to next block; PUSH clamps to 0 |
| kQueueCap = 64 (not 8) | Supports ROLL×8 + STUTTER×8 combined |
| triggerRetain() on MonoSynth | STUTTER sub-events 2..N have no phase reset → no click |
| drain THEN resetFractureSeed | FRACTURE determinism preserved across bar boundaries |
| Sample banks registered (v2) | .layerzbank JSON format, local dirs, SampleVoice post-F4 |

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/04-groove-engine/04-03-PLAN.md` | Current plan (checkpoint pending) |
| `Source/audio/AestheticResolver.h/.cpp` | Core groove engine |
| `Source/gui/GroovePanel.h/.cpp` | Template selector + morph knob |
| `docs/spikes/F3.99-groove-accuracy.md` | Strategy B decision |
| `ROADMAP.md` | Sample banks v2 spec added |

---

## Resume Instructions

1. Run LAYERZ: `open build/LAYERZ_artefacts/Standalone/LAYERZ.app`
   (clear saved state first: `rm ~/.../LAYERZ.settings`)
2. Ask user to test groove engine and confirm checkpoint
3. On "approved": write SUMMARY files and run `/paul:unify`

Or run: `/paul:resume` — it reads STATE.md and restores position automatically.

---

*Handoff created: 2026-05-30*
